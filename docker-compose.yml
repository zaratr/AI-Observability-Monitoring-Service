version: "3.9"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./observability.db
      - LLM_PROVIDER=dummy
    volumes:
      - .:/app
